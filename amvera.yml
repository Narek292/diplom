meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12

build:
  requirementsPath: requirements.txt

run:
  command: python manage.py migrate && python manage.py collectstatic --noinput && gunicorn devicemanage.wsgi:application --bind 0.0.0.0:80

persistence:
  - mountPath: db.sqlite3
    name: sqlite-db